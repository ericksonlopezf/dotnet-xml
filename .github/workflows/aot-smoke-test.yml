# Copyright © Erickson Lopez. MIT License.
name: NativeAOT Smoke Test

on:
  workflow_call:
    secrets:
      SNK_KEY:
        description: "Base64-encoded Strong Name key (.snk) for assembly signing"
        required: false
  push:
    branches: [ main, develop ]
  pull_request:
    branches: [ main, develop ]
  workflow_dispatch:

env:
  DOTNET_NOLOGO: true
  DOTNET_SKIP_FIRST_TIME_EXPERIENCE: true

jobs:
  aot-smoke-test:
    name: NativeAOT Smoke Test
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1

      - name: Setup .NET
        uses: actions/setup-dotnet@67a3573c9a986a3f9c594539f4ab511d57bb3ce9 # v4.3.1
        with:
          dotnet-version: |
            8.0.x
            9.0.x
            10.0.x

      - name: Restore Strong Name key
        env:
          SNK_KEY: ${{ secrets.SNK_KEY }}
        run: |
          if [ -n "$SNK_KEY" ]; then
            echo "$SNK_KEY" | tr -d '\n\r ' | base64 --decode > EricksonLopez.snk
          fi

      - name: Install NativeAOT prerequisites (clang, lld, zlib)
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y --no-install-recommends clang lld zlib1g-dev

      - name: Restore dependencies
        run: dotnet restore EricksonLopez.Xml.slnx

      - name: Build (Release)
        run: dotnet build EricksonLopez.Xml.slnx --no-restore --configuration Release

      # ─── Publish with AOT ────────────────────────────────────────────────
      - name: Publish AotSmokeTest (NativeAOT)
        run: >
          dotnet publish
          tests/EricksonLopez.Xml.Validation.AotTest/EricksonLopez.Xml.Validation.AotTest.csproj
          --configuration Release
          --runtime linux-x64
          --self-contained
          -p:TreatWarningsAsErrors=true
          -p:WarningLevel=5
          --output ./aot-output
        env:
          DOTNET_EnableAotCompilationWarningsAsErrors: true

      # ─── Run the native binary ───────────────────────────────────────────
      - name: Run NativeAOT binary
        run: |
          chmod +x ./aot-output/EricksonLopez.Xml.Validation.AotTest
          ./aot-output/EricksonLopez.Xml.Validation.AotTest
          echo "Exit code: $?"

      # ─── Upload binary for debugging if needed ────────────────────────────
      - name: Upload AOT artifacts (on failure)
        if: failure()
        uses: actions/upload-artifact@ea165f8d65b6e75b540449e92b4886f43607fa02 # v4.6.2
        with:
          name: aot-output-${{ github.run_id }}
          path: ./aot-output/
          retention-days: 7
